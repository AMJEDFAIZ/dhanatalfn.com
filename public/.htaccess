#########################################################
# ملف: public/.htaccess
# هذا هو ملف Laravel الداخلي الموجود داخل مجلد public
# وظيفته: إدارة الطلبات بعد وصولها إلى تطبيق Laravel
#########################################################

<IfModule mod_rewrite.c>

    # منع تعدد طرق الوصول للملفات + منع استعراض المجلدات
    <IfModule mod_negotiation.c>
        Options -MultiViews -Indexes
    </IfModule>

    # تفعيل إعادة الكتابة (أساسي لعمل Laravel)
    RewriteEngine On

    #####################################################
    # 1) التعامل مع الهيدرز الأمنية الخاصة بالمصادقة
    #####################################################

    # تمرير هيدر Authorization إلى PHP/Laravel
    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    # تمرير هيدر X-XSRF-TOKEN إلى Laravel
    RewriteCond %{HTTP:x-xsrf-token} .
    RewriteRule .* - [E=HTTP_X_XSRF_TOKEN:%{HTTP:X-XSRF-Token}]

    #####################################################
      # تحسينات الأداء (Gzip & Caching) - SEO
    #####################################################

    # تفعيل ضغط Gzip
    <IfModule mod_deflate.c>
        AddOutputFilterByType DEFLATE text/plain
        AddOutputFilterByType DEFLATE text/html
        AddOutputFilterByType DEFLATE text/xml
        AddOutputFilterByType DEFLATE text/css
        AddOutputFilterByType DEFLATE application/xml
        AddOutputFilterByType DEFLATE application/xhtml+xml
        AddOutputFilterByType DEFLATE application/rss+xml
        AddOutputFilterByType DEFLATE application/javascript
        AddOutputFilterByType DEFLATE application/x-javascript
        AddOutputFilterByType DEFLATE application/json
        AddOutputFilterByType DEFLATE image/svg+xml
    </IfModule>

    # تفعيل تخزين المتصفح (Browser Caching)
    <IfModule mod_expires.c>
        ExpiresActive On
        ExpiresByType image/jpg "access plus 1 year"
        ExpiresByType image/jpeg "access plus 1 year"
        ExpiresByType image/gif "access plus 1 year"
        ExpiresByType image/png "access plus 1 year"
        ExpiresByType image/webp "access plus 1 year"
        ExpiresByType image/svg+xml "access plus 1 year"
        ExpiresByType text/css "access plus 1 month"
        ExpiresByType application/pdf "access plus 1 month"
        ExpiresByType text/x-javascript "access plus 1 month"
        ExpiresByType application/x-shockwave-flash "access plus 1 month"
        ExpiresByType image/x-icon "access plus 1 year"
        ExpiresDefault "access plus 2 days"
    </IfModule>

    #####################################################
    # 2) تنظيف الروابط من السلاش الزائد في النهاية
    #####################################################

    # إذا كان الطلب ينتهي بـ "/" وليس مجلدًا حقيقيًا:
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} (.+)/$
    RewriteRule ^ %1 [L,R=301]

    #####################################################
    # 3) توجيه كل الطلبات غير الموجودة إلى index.php
    #    (قلب نظام Laravel)
    #####################################################

    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]

</IfModule>

#########################################################
# 4) السماح بالوصول إلى الملفات داخل public/storage
#    (حتى يمكن عرض الصور المخزنة)
#########################################################

<IfModule mod_authz_core.c>
    Require all granted
</IfModule>
